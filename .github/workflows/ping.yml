name: Keep Render Alive

on:
  schedule:
    - cron: "*/5 * * * *" # Every 5 mins lol
    workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend health route
        run: curl https://question-paper-generator-k4ed.onrender.com/health
  